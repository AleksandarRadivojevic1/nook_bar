---
// Sections are added here, in order, by Tasks 7 and 9-14.
import type { Locale } from '../i18n';

interface Props {
  locale: Locale;
}

const { locale } = Astro.props;
---

<main data-locale={locale}></main>

<script>
  import { motion } from '../scripts/motion';
  motion();
</script>
